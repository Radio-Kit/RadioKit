# RadioKit Remote Access API

RadioKit is an Arduino library for building wireless UI controllers. The Flutter companion app exposes a REST API for device control and firmware documentation.

## Base URL

http://<app-ip>:7007

## Quick Start

1. GET /api/status - verify server is running
2. POST /api/connection/connect - connect to a device
3. GET /api/widgets - list available widgets
4. PUT /api/widgets/<id> - control widget values

## Endpoints

### Server
- GET /api/status - Server health, version, platform

### Connection
- GET /api/connection - Connection state and device info
- POST /api/connection/connect - Connect to device (body: id, type)
- POST /api/connection/disconnect - Disconnect
- POST /api/connection/reconnect - Auto-reconnect
- POST /api/connection/switch - Switch transport (body: transport)
- POST /api/connection/demo - Load demo (body: demoId)

### Widgets
- GET /api/widgets - List all widgets with state
- GET /api/widgets/<id> - Get single widget
- PUT /api/widgets/<id> - Set widget value (body: values)

### Filesystem
- GET /api/fs/list?path=/ - List directory
- GET /api/fs/info - Storage usage
- GET /api/fs/read?path=/file - Read file (base64)
- POST /api/fs/write - Write file (body: path, data)
- POST /api/fs/upload - Upload file (body: path, data)
- POST /api/fs/mkdir - Create directory (body: path)
- POST /api/fs/delete - Delete file (body: path, recursive)
- POST /api/fs/rename - Rename file (body: oldPath, newPath)
- POST /api/fs/format - Format filesystem

### OTA
- POST /api/ota/upload - Upload firmware (body: data, eraseAll)
- GET /api/ota/progress - Upload progress

### Console
- GET /api/console - Log entries
- DELETE /api/console - Clear log

### Device Config
- GET /api/settings/nvs - Read device config
- POST /api/settings/nvs - Write config (body: name, description, password)
- POST /api/settings/nvs/authenticate - Auth (body: password)
- POST /api/settings/nvs/reboot - Reboot device
- POST /api/settings/nvs/factory-reset - Erase and reboot (body: confirm: true)

### Multi-Device
- GET /api/devices - List connected devices
- POST /api/devices/connect - Connect device (body: id, type)
- POST /api/devices/disconnect - Disconnect device (body: id)
- GET /api/devices/<id> - Device info
- GET /api/devices/<id>/widgets - Device widgets
- GET /api/devices/<id>/widgets/<wid> - Get device widget
- PUT /api/devices/<id>/widgets/<wid> - Set widget value
- GET /api/devices/<id>/console - Device console log
- DELETE /api/devices/<id>/console - Clear device console
- GET /api/devices/<id>/fs/list - Device FS list
- GET /api/devices/<id>/fs/info - Device FS info
- GET /api/devices/<id>/fs/read - Device FS read
- POST /api/devices/<id>/fs/write - Device FS write
- POST /api/devices/<id>/fs/upload - Device FS upload
- POST /api/devices/<id>/fs/mkdir - Device FS mkdir
- POST /api/devices/<id>/fs/delete - Device FS delete
- POST /api/devices/<id>/fs/rename - Device FS rename
- POST /api/devices/<id>/fs/format - Device FS format
- POST /api/devices/<id>/fs/probe - Device FS probe
- POST /api/devices/<id>/ota/upload - Device OTA upload
- GET /api/devices/<id>/ota/progress - Device OTA progress
- POST /api/devices/<id>/transport/send - Device transport send
- POST /api/devices/<id>/transport/ping - Device transport ping
- POST /api/devices/<id>/transport/wifi_info - Device WiFi info
- POST /api/devices/<id>/transport/<cmd> - Device transport command
- GET /api/devices/<id>/settings/nvs - Device NVS read
- POST /api/devices/<id>/settings/nvs - Device NVS write
- POST /api/devices/<id>/settings/nvs/authenticate - Device auth
- POST /api/devices/<id>/settings/nvs/factory-reset - Device factory reset
- POST /api/devices/<id>/settings/nvs/reboot - Device reboot
- GET /api/devices/<id>/settings/nvs/raw/<key> - Device raw NVS read
- POST /api/devices/<id>/settings/nvs/raw/<key> - Device raw NVS write
- GET /api/devices/<id>/settings/nvs/cloud-info - Device cloud info

### Serial Flasher
- GET /api/flasher/ports - List serial ports
- POST /api/flasher/scan - Scan ports
- POST /api/flasher/connect - Connect port (body: portId)
- POST /api/flasher/disconnect - Disconnect port
- POST /api/flasher/flash - Start flash
- GET /api/flasher/status - Flash status
- GET /api/flasher/log - Flash log
- POST /api/flasher/log/clear - Clear flash log
- POST /api/flasher/select-firmware - Select firmware file
- POST /api/flasher/clear-firmware - Clear selected firmware
- POST /api/flasher/erase-all - Erase all flash

### Cloud Relay
- POST /api/cloud/connect - Connect relay (body: host, port, account, privateKey)
- GET /api/cloud/devices - Cloud device list
- POST /api/cloud/join - Join device (body: device)
- POST /api/cloud/disconnect - Disconnect relay
- GET /api/cloud/account - Get Ed25519 identity (public key)
- POST /api/cloud/account - Generate new Ed25519 identity
- GET /api/cloud/accounts - List all cloud accounts
- POST /api/cloud/accounts - Create named account (body: name, relay)
- PUT /api/cloud/accounts/<id> - Update account (body: name, relay)
- DELETE /api/cloud/accounts/<id> - Delete cloud account

### Page Management
- GET /api/page - Get active page (returns activePage, numPages, pages)
- POST /api/page - Set active page (body: page)
- GET /api/pages - Get all pages (returns pages, numPages, activePage)

### Session
- GET /api/session/route - Current GoRouter location
- GET /api/session/state - Session state
- GET /api/session/sheets - Session sheets

### Device Settings (NVS)
- GET /api/settings - Get device config
- PUT /api/settings - Update device config
- GET /api/settings/nvs - Read NVS
- POST /api/settings/nvs - Write NVS (body: name, description, password)
- POST /api/settings/nvs/authenticate - Auth (body: password)
- POST /api/settings/nvs/factory-reset - Erase and reboot (body: confirm: true)
- POST /api/settings/nvs/reboot - Reboot device
- GET /api/settings/nvs/raw/<key> - Raw NVS read
- POST /api/settings/nvs/raw/<key> - Raw NVS write
- GET /api/settings/nvs/cloud-info - Cloud info from NVS

### Transport
- POST /api/transport/send - Send raw frame
- POST /api/transport/ping - Ping device
- POST /api/transport/wifi_info - WiFi info
- POST /api/transport/<cmd> - Quick command

### Log
- GET /api/log - App log entries
- DELETE /api/log - Clear app log

### Models (Paired Devices)
- GET /api/models - List paired models
- DELETE /api/models - Delete all paired models
- DELETE /api/models/<id> - Delete paired model

### Designs
- GET /api/designs - List all saved designs
- POST /api/designs - Save a design (body: id, name, jsonContent)
- GET /api/designs/<id>/json - Get design JSON config
- GET /api/designs/<id>/header - Fetch RADIOKIT.h file (text/plain)
- DELETE /api/designs/<id> - Delete a design

## Firmware Code Generation

To generate firmware from a saved design:
1. GET /api/designs - list designs, pick one by id
2. GET /api/designs/<id>/header - fetch the RADIOKIT.h file
3. Use the fetched content as the device's header file

## Documentation

- GET /api/docs - List all documentation files
- GET /api/docs/<skill> - Read specific skill doc (markdown)
- GET /api/docs/api-schema - Full API schema as JSON

### Available Skills
- radiokit-firmware: Complete firmware development guide
- radiokit-widgets: All widget types and usage patterns
- radiokit-transports: BLE, Serial, WiFi, Cloud setup
- radiokit-filesystem: LittleFS file operations
- radiokit-ota: Over-the-air firmware updates
- radiokit-remote: This API reference and autonomous agent workflows
